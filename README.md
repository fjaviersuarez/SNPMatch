# SNPMatch

RScript to merge .snp files and summary statistics after a PRS to obtain a file with the LeadSNPs and their rsID

## Usage
```
Rscript SNPMatch.R --snp file.snp --summary summary_statistics_file_with_rsID_and_bp_hg19.txt --out merged_file
```
